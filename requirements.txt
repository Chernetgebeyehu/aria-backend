# ARIA Backend — Python Dependencies v3.1.0
# ============================================
# Install all: pip install -r requirements.txt

# ─── CORE SERVER ──────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0
python-dotenv==1.0.1
pydantic==2.7.0

# ─── MEMORY SYSTEM ────────────────────────────────────────────────────────────
# These were accidentally removed — adding them back.
# numpy<2.0 is REQUIRED — chromadb 0.5.0 was built for NumPy 1.x.
# Without this pin, Railway installs NumPy 2.0 and ChromaDB crashes.
numpy<2.0
chromadb==0.5.0
sentence-transformers==3.0.0
torch==2.3.0